diff --git javascript/media-admin.js javascript/media-admin.js
index e244400..297ee40 100644
--- javascript/media-admin.js
+++ javascript/media-admin.js
@@ -123,11 +123,11 @@ Drupal.behaviors.mediaTypesAdmin = {
       return;
     }
     // Toggle the 'other' text field on Match type.
-    if ($('.form-item-match-type input:checked').val() != '0') {
+    if ($('.form-item-match-type input:checked').val() != 'other') {
       $('.form-item-match-type-other').hide();
     }
     $('.form-item-match-type input').change(function () {
-      if ($(this).val() == '0') {
+      if ($(this).val() == 'other') {
         $('.form-item-match-type-other').slideDown('fast');
       }
       else {
diff --git media.admin.inc media.admin.inc
index b9a1bc9..efd2ada 100644
--- media.admin.inc
+++ media.admin.inc
@@ -345,14 +345,14 @@ function media_admin_type_manage_form($form, &$form_state, $media_type) {
     $options = array(
       'all' => t('All'),
       'any' => t('Any'),
-      '0' => t('Other'),
+      'other' => t('Other'),
     );
     if ($media_type->type_callback_args['match_type'] && isset($options[$media_type->type_callback_args['match_type']])) {
       $default_value = $media_type->type_callback_args['match_type'];
       $other_default_value = '';
     }
     else {
-      $default_value = 0;
+      $default_value = 'other';
       $other_default_value = $media_type->type_callback_args['match_type'];
     }
     $form['match_type'] = array(
